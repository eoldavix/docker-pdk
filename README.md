# Puppet Development Kit Validation tool
Docker image for Puppet Development Kit Validation tool

## How to
Execute into your Puppet module path:
```docker run --rm -ti -v ${PWD}:/app eoldavix/pdk-validate```
